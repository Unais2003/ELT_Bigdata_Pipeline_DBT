# in production it can be a data lake, data warehouse, or even an API. 
# in this case we are using raw. 
version: 2
sources:
  - name: raw
    database: nyc_taxi
    schema: raw

    tables: 
      - name: yellow_taxi_trips

      - name: taxi_zone_lookup